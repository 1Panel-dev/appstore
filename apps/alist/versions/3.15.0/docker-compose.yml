version: '3'
services:
  alist:
    restart: always
    volumes:
        - './data:/opt/alist/data'
    ports:
        - '${PANEL_APP_PORT_HTTP}:5244'
    # environment:
    #     - PUID=0
    #     - PGID=0
    #     - UMASK=022
    container_name: alist
    image: 'xhofe/alist:v3.15.0'
  labels:
    createdBy: "Apps"

networks:  
  1panel-network:  
    external: true
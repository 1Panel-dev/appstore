version: "3"
services:
  one-api:
    image: justsong/one-api:v0.5.11
    container_name: ${CONTAINER_NAME}
    restart: always
    networks:
      - 1panel-network
    volumes:
      - ./data:/data
    ports:
      - ${PANEL_APP_PORT_HTTP}:3000
    environment:
      - TZ=${TZ}
      - SQL_DSN=${SQL_DSN}
    labels:
      createdBy: "igophper"
networks:
  1panel-network:
    external: true
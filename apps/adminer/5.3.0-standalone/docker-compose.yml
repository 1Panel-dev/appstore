services:  
  adminer:  
    image: adminer:5.4.0-standalone 
    container_name: ${CONTAINER_NAME}
    restart: always  
    networks:  
      - 1panel-network
    ports:  
      - ${PANEL_APP_PORT_HTTP}:8080  
    labels:  
      createdBy: "Apps"  
networks:  
  1panel-network:  
    external: true

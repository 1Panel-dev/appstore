## 产品介绍

**Valkey** 是一个高性能开源数据结构服务器，主要面向键值型工作负载，同时支持多种原生数据结构，并提供可扩展的插件系统。

## 主要特性

- **键值与数据结构**：适用于缓存、会话存储、消息队列和高频读写等场景。
- **高性能**：专注于高吞吐、低延迟的数据访问体验。
- **原生扩展能力**：支持丰富的数据结构，并可通过插件系统扩展新的数据结构和访问模式。
- **生态兼容**：提供 `valkey-server`、`valkey-cli` 等常用组件，便于本地测试和应用接入。

version: '3.1'
services:
  testing:
    image: "docker.io/linuxsuren/api-testing:v0.0.17"
    container_name: ${CONTAINER_NAME}
    restart: always
    ports:
      - 8080:8080
    labels:  
      createdBy: "Apps"  

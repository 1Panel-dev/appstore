version: '3'
services:
  ddns-go:
    image: jeessy/ddns-go
    container_name: ddns-go
    ports:
      - ${port}:9876
    volumes:
      - /opt/ddns-go:/root
    restart: always
    network_mode: host
